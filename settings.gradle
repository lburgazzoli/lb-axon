rootProject.name = "lb-axon"

include "axon-common"
include "axon-hazelcast"
include "axon-chronicle"
include "axon-osgi"
include "axon-samples"

rootProject.children.each { 
    it.name = "lb-" + it.name 
}
